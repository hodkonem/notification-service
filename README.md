# Notification Service

`notification-service` — микросервис уведомлений, предназначенный для обработки событий пользователей,
поступающих из Apache Kafka, и отправки email-уведомлений.

Проект является частью учебной микросервисной системы и демонстрирует использование:
- Apache Kafka
- Spring Boot
- Spring Cloud Config
- Testcontainers
- Интеграционных тестов с Kafka и SMTP

---

## 📌 Назначение сервиса

Сервис подписывается на Kafka-топик с событиями пользователей и реагирует на них:

- `USER_CREATED` → отправка email об успешном создании пользователя
- `USER_DELETED` → отправка email об удалении пользователя

Источник событий — внешний сервис (например, `user-service`).

---

## 🧩 Архитектура

```

[user-service]
|
|  Kafka (user.notifications)
v
[notification-service] ---> SMTP (MailHog / GreenMail)

````

---

## 🛠 Технологии

- **Java 21**
- **Spring Boot 3.5.x**
- **Spring Kafka**
- **Spring Mail**
- **Spring Cloud Config (Config Client)**
- **Springdoc OpenAPI**
- **Apache Kafka**
- **Testcontainers**
- **GreenMail (SMTP для тестов)**
- **Gradle**

---

## ⚙️ Конфигурация

Сервис использует **Spring Cloud Config** и загружает конфигурацию из внешнего Config Server.

### application.yml (минимальный)

```yaml
spring:
  application:
    name: notification-service

  config:
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}

  cloud:
    config:
      fail-fast: false
      label: develop
````

### Основные параметры (из config-repo)

* `spring.kafka.bootstrap-servers`
* `app.kafka.topic`
* `spring.mail.host`
* `spring.mail.port`
* `app.mail.from`

---

## 📬 Kafka

* **Топик:** `user.notifications`
* **Формат сообщений:** JSON
* **DTO:** `UserEventDto`

```json
{
  "operation": "CREATED",
  "email": "user@example.com"
}
```

### Kafka Listener

```java
@KafkaListener(topics = "${app.kafka.topic:user.notifications}")
public void onMessage(String payload) { }
```

---

## ✉️ Email

Для отправки писем используется `Spring Boot Mail`.

* В dev-окружении можно использовать **MailHog**
* В тестах используется **GreenMail**

---

## 🧪 Тестирование

### Unit-тесты

* Проверка загрузки контекста
* Проверка конфигурации

### Интеграционные тесты

* Kafka поднимается через **Testcontainers**
* SMTP поднимается через **GreenMail**
* Проверяется полный сценарий:

  1. Отправка события в Kafka
  2. Обработка события listener’ом
  3. Отправка email
  4. Проверка полученного письма

Пример:

```bash
./gradlew clean test
```

---

## 🧠 Особенности реализации

* Kafka listener включается/выключается через `app.kafka.enabled`
* В тестах отключён Spring Cloud Config
* Конфигурация Kafka и Mail полностью переопределяется для IT-тестов
* Listener устойчив к ошибкам десериализации

---

## 📖 Swagger / OpenAPI

Swagger UI доступен (в dev-профиле):

```
http://localhost:8085/swagger-ui
```

В тестах Swagger отключён.

---

## 🚀 Запуск локально

1. Запустить Kafka и SMTP (например, через docker-compose)
2. Запустить Config Server
3. Запустить сервис:

```bash
./gradlew bootRun
```

---

## 📂 Связанные проекты

* `user-service` — источник Kafka-событий
* `config-server` — Spring Cloud Config Server
* `config-repo` — централизованный репозиторий конфигураций

---

## 📌 Статус

✔ Реализован
✔ Покрыт интеграционными тестами
✔ Использует Spring Cloud Config
✔ Готов к расширению (Eureka, Gateway, Circuit Breaker)

---

## 👤 Автор

Проект выполнен в рамках учебной практики по Java и Spring экосистеме.